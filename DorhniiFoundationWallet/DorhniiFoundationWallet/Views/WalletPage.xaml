<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="DorhniiFoundationWallet.Views.WalletPage"
             xmlns:resource="clr-namespace:DorhniiFoundationWallet.Resources"
             xmlns:views="clr-namespace:DorhniiFoundationWallet.Views.Partials"
             xmlns:vm="clr-namespace:DorhniiFoundationWallet.ViewModels"
             xmlns:cntrl="clr-namespace:DorhniiFoundationWallet.CustomRenderers"
             xmlns:ios="clr-namespace:Xamarin.Forms.PlatformConfiguration.iOSSpecific;assembly=Xamarin.Forms.Core"
             ios:Page.UseSafeArea="true"
             NavigationPage.HasNavigationBar="False">
    <ContentPage.BindingContext>
        <vm:WalletVM x:Name="WalletPageVM"/>
    </ContentPage.BindingContext>
    <ContentPage.Content>
        <Grid HorizontalOptions="FillAndExpand" VerticalOptions="FillAndExpand" >
            <Grid RowSpacing="0">
                <Grid.RowDefinitions>
                    <RowDefinition Height="160"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                <Grid Grid.Row="0" Margin="10,10,10,0" >
                    <Grid.RowDefinitions>
                        <RowDefinition Height="50"/>
                        <RowDefinition Height="30"/>
                        <RowDefinition Height="20"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width=".3*"/>
                    </Grid.ColumnDefinitions>
                    <Label Grid.Row="0" Grid.Column="0" HorizontalOptions="CenterAndExpand" VerticalOptions="Center" HorizontalTextAlignment="Center" FontSize="Large" FontAttributes="Bold" Text="Wallet" TextColor="Black" IsVisible="{Binding Wallet}"  />
                    <cntrl:RendererEntry Grid.Row="0" Grid.Column="0" Placeholder="WalletName" PlaceholderColor="White" HorizontalOptions="CenterAndExpand" VerticalOptions="Center" HorizontalTextAlignment="Center" FontSize="Large" FontAttributes="Bold" IsVisible="{Binding Edit }" />
                    <StackLayout Grid.Row="0" Grid.Column="0" HorizontalOptions="EndAndExpand" Margin="5,10,0,0" IsVisible="{Binding EditButton}">
                        <Image Source="{Binding EditIcon}" HeightRequest="30" WidthRequest="30" />
                        <StackLayout.GestureRecognizers>
                            <TapGestureRecognizer Command="{Binding EditButtonCommand}"/>
                        </StackLayout.GestureRecognizers>
                    </StackLayout>
                    <StackLayout Grid.Row="0" Grid.Column="0" HorizontalOptions="EndAndExpand" Margin="5,10,0,0" IsVisible="{Binding SaveButton}">
                        <Image Source="{Binding SaveIcon}" HeightRequest="30" WidthRequest="30" />
                        <StackLayout.GestureRecognizers>
                            <TapGestureRecognizer Command="{Binding SaveButtonCommand}"/>
                        </StackLayout.GestureRecognizers>
                    </StackLayout>
                    <Label Grid.Row="1" Text="Balance" Style="{StaticResource WalletPageLabel}" FontSize="Large" TextColor="Black" />
                    <Label Grid.Row="2" Text="{Binding WalletBalance}" Style="{StaticResource WalletPageLabel}" FontSize="Medium" TextColor="Black"/>
                </Grid>
                <Grid Grid.Row="1" RowSpacing="8"  HorizontalOptions="FillAndExpand" VerticalOptions="CenterAndExpand" Margin="10,10,10,10">
                    <ListView  ItemsSource="{Binding WalletList}" HorizontalOptions="FillAndExpand"  RowHeight="100" VerticalOptions="CenterAndExpand" SeparatorVisibility="Default" HasUnevenRows="True" SelectionMode="None" >
                            <ListView.ItemTemplate>
                                <DataTemplate>
                                    <ViewCell>
                                        <Frame Style="{StaticResource WalletFrameStyle2}" Margin="0,10,0,0" >
                                            <Grid>
                                                <StackLayout  Margin=" 10,5,0,5">
                                                <Label Text="{Binding coinName}" Style="{StaticResource WalletPageListLabel}" TextColor="Black"/>
                                                <Label Text="{Binding coinValue}" Style="{StaticResource WalletPageListLabel}" TextColor="Black"/>
                                                </StackLayout>
                                            </Grid>
                                        </Frame>
                                </ViewCell>
                                </DataTemplate>
                            </ListView.ItemTemplate>
                        </ListView>
                </Grid>
                <views:TabbedViewPage Grid.Row="2" HorizontalOptions="FillAndExpand"  BackgroundColor="Transparent"></views:TabbedViewPage>
            </Grid>
        </Grid>
    </ContentPage.Content>
</ContentPage>