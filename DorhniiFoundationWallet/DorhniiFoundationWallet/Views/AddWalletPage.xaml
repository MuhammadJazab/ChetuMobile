<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:vm="clr-namespace:DorhniiFoundationWallet.ViewModels" 
             xmlns:cntrl="clr-namespace:DorhniiFoundationWallet.CustomRenderers"
             NavigationPage.HasNavigationBar="False"
             x:Class="DorhniiFoundationWallet.Views.AddWalletPage">
    <ContentPage.BindingContext>
        <vm:AddWalletVM/>
    </ContentPage.BindingContext>
    <ContentPage.Content>
        <Grid HorizontalOptions="FillAndExpand" VerticalOptions="FillAndExpand" BackgroundColor="White">
            <Grid RowSpacing="0">
                <Grid.RowDefinitions>
                    <RowDefinition Height="50"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                <Grid Grid.Row="0" BackgroundColor="SteelBlue">
                    <Label Text="My Wallet" FontSize="Medium" TextColor="White" FontAttributes="Bold" HorizontalOptions="CenterAndExpand" VerticalOptions="CenterAndExpand"/>
                </Grid>
                <Grid Grid.Row="1" RowSpacing="8"  HorizontalOptions="FillAndExpand" VerticalOptions="CenterAndExpand" Margin="10,10,10,10">
                    <ListView x:Name="WalletList" ItemsSource="{Binding Wallets}" HorizontalOptions="FillAndExpand"  RowHeight="100" VerticalOptions="CenterAndExpand" SeparatorVisibility="Default" HasUnevenRows="True" >
                        <ListView.ItemTemplate>
                            <DataTemplate>
                                <ViewCell>
                                    <Grid>
                                        <Frame Style="{StaticResource WalletFrameStyle2}" Margin="0,10,0,0" >
                                            <Grid>
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="auto"/>
                                                    <ColumnDefinition Width="*"/>
                                                </Grid.ColumnDefinitions>
                                                <Image VerticalOptions="Center" Grid.Column="0" Margin="10,10,0,0" Source="{Binding Image}" HeightRequest="35"/>
                                                <StackLayout Grid.Column="1" BackgroundColor="Transparent" Margin=" 10,5,0,5">
                                                    <Label Text="{Binding WalletName}" Style="{StaticResource WalletPageListLabel}" TextColor="Black"/>
                                                    <Label Text="{Binding Balance}" Style="{StaticResource WalletPageListLabel}" TextColor="Black"/>
                                                </StackLayout>
                                                <Grid.GestureRecognizers>
                                                    <TapGestureRecognizer Command="{Binding BindingContext.SelectedItemCommand, Source={x:Reference WalletList}}" CommandParameter="{Binding}"/>
                                                </Grid.GestureRecognizers>
                                            </Grid>
                                        </Frame>
                                    </Grid>
                                </ViewCell>
                            </DataTemplate>
                        </ListView.ItemTemplate>
                    </ListView>
                </Grid>
                <Button Grid.Row="2"  Text="Add Wallet" HorizontalOptions="FillAndExpand" VerticalOptions="EndAndExpand" TextColor="White" FontAttributes="Bold" BackgroundColor="SteelBlue" Command="{Binding AddWalletCommand}"/>
            </Grid>
            <Grid HorizontalOptions="FillAndExpand" VerticalOptions="FillAndExpand" BackgroundColor="LightGray" IsVisible="{Binding IsCreateWalletVisible }">
                <Frame Grid.Row="0" Margin="20" Padding="0" CornerRadius="10"  HorizontalOptions="FillAndExpand" VerticalOptions="CenterAndExpand" >
                    <Grid HorizontalOptions="FillAndExpand" VerticalOptions="FillAndExpand">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="40"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>
                        <Grid Grid.Row="0" BackgroundColor="SteelBlue">
                            <Label Text="Add Wallet" FontSize="Medium" TextColor="White" FontAttributes="Bold" HorizontalOptions="CenterAndExpand" VerticalOptions="CenterAndExpand"/>
                        </Grid>
                        <Frame Grid.Row="1" Margin="20"  Style="{StaticResource PasswordFrameStyle}">
                            <cntrl:RendererEntry Text="{Binding WalletName}" Style="{StaticResource WalletPageEntryStyle}" HeightRequest="100" Placeholder="Wallet name" />
                        </Frame>
                        <Button Command="{Binding CreateWalletCommand}" Grid.Row="2" Margin="20"  Text="Create Wallet" HorizontalOptions="FillAndExpand" TextColor="White" FontAttributes="Bold" BackgroundColor="SteelBlue"/>
                    </Grid>
                </Frame>
                <Grid.GestureRecognizers>
                    <TapGestureRecognizer Command="{Binding CloseCreateWalletCommand}"/>
                </Grid.GestureRecognizers>
            </Grid>
        </Grid>
    </ContentPage.Content>
</ContentPage>