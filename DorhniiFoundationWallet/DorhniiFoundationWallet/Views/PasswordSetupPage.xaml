<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:resource="clr-namespace:DorhniiFoundationWallet.Resources"
             xmlns:vm="clr-namespace:DorhniiFoundationWallet.ViewModels"
             xmlns:cntrl="clr-namespace:DorhniiFoundationWallet.CustomRenderers"
             xmlns:ios="clr-namespace:Xamarin.Forms.PlatformConfiguration.iOSSpecific;assembly=Xamarin.Forms.Core"
             ios:Page.UseSafeArea="true"
             NavigationPage.HasNavigationBar="False"
             x:Class="DorhniiFoundationWallet.Views.PasswordSetupPage" >
    <ContentPage.BindingContext>
        <vm:PasswordSetupVM/>
    </ContentPage.BindingContext>
    <ContentPage.Content>
        <Grid HorizontalOptions="FillAndExpand" VerticalOptions="FillAndExpand">
            <Grid HorizontalOptions="FillAndExpand" Margin="10,0,10,0">
                <Grid.RowDefinitions>
                    <RowDefinition Height=".3*"/>
                    <RowDefinition Height=".7*"/>
                </Grid.RowDefinitions>
                <StackLayout Grid.Row="0" Spacing="0" VerticalOptions="Center">
                    <Image Source="{Binding AppIcon}" Aspect="AspectFit" Style="{StaticResource AppIconImageStyle}"/>
                </StackLayout>
                <Grid Grid.Row="1" VerticalOptions="StartAndExpand" HorizontalOptions="FillAndExpand">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <Label Grid.Row="0" Text="{x:Static resource:Resource.txtPassword}" Style="{StaticResource CenterBlackLabelStyle}" />
                    <Frame Grid.Row="1" Style="{StaticResource PasswordFrameStyle}" >
                        <cntrl:RendererEntry Text="{Binding Password}" Placeholder="{x:Static resource:Resource.txtPassword}" Keyboard="Default" FontAttributes="None" Style="{StaticResource PasswordEntryStyle}"  HorizontalOptions="Fill" />
                    </Frame>
                    <Frame Grid.Row="2" Style="{StaticResource PasswordFrameStyle}" >
                        <cntrl:RendererEntry Text="{Binding ConfirmPassword}" Placeholder="{x:Static resource:Resource.txtConfirmPassword}" Keyboard="Default" FontAttributes="None" Style="{StaticResource PasswordEntryStyle}" HorizontalOptions="Fill" />
                    </Frame>
                    <Button Grid.Row="3" Text="{x:Static resource:Resource.txtCreate}" Command="{Binding CreatePasswordCommand}" Style="{StaticResource SteelBlueButtonsStyle}" />
                    <Grid Grid.Row="4" RowSpacing="0">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="auto"/>
                            <RowDefinition Height="auto"/>
                            <RowDefinition Height="auto"/>
                        </Grid.RowDefinitions>
                        <Label Grid.Row="0" Style="{StaticResource CenterBlackLabelStyle}" Text="{x:Static resource:Resource.txtPasswordPageText1}" FontSize="14"/>
                        <Label Grid.Row="1" Style="{StaticResource CenterBlackLabelStyle}" Text="{x:Static resource:Resource.txtPasswordPageText2}" FontSize="14"/>
                        <Label Grid.Row="2" Style="{StaticResource CenterBlackLabelStyle}" Text="{x:Static resource:Resource.txtPasswordPageText3}" FontSize="14"/>
                    </Grid>
                </Grid>
            </Grid>
        </Grid>
    </ContentPage.Content>
</ContentPage>