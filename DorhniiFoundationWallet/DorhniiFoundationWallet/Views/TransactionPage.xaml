<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:vm="clr-namespace:DorhniiFoundationWallet.ViewModels"
             xmlns:resource="clr-namespace:DorhniiFoundationWallet.Resources"
             xmlns:views="clr-namespace:DorhniiFoundationWallet.Views.Partials"
             x:Class="DorhniiFoundationWallet.Views.TransactionPage"
             NavigationPage.HasNavigationBar="False">
    <ContentPage.BindingContext>
        <vm:TransactionVM/>
    </ContentPage.BindingContext>
    <ContentPage.Content>
        <Grid HorizontalOptions="FillAndExpand" VerticalOptions="FillAndExpand" BackgroundColor="White">
            <Grid RowSpacing="0">
                <Grid.RowDefinitions>
                    <RowDefinition Height="50"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                <Grid Grid.Row="0" Margin="10,10,10,0" >
                    <Grid.RowDefinitions>
                        <RowDefinition Height="50"/>
                    </Grid.RowDefinitions>
                    <Label Grid.Row="0" Text="{x:Static resource:Resource.txtTransactions}" Style="{StaticResource PasswordPageLabelStyle}" TextColor="Black"/>
                </Grid>
                <Grid Grid.Row="1" RowSpacing="8"  HorizontalOptions="FillAndExpand" VerticalOptions="CenterAndExpand" Margin="10,10,10,10">
                    <ListView ItemsSource="{Binding TransactionList}" HorizontalOptions="FillAndExpand"  RowHeight="100" VerticalOptions="CenterAndExpand" SeparatorVisibility="Default" HasUnevenRows="True" SelectionMode="None" >
                        <ListView.ItemTemplate>
                            <DataTemplate>
                                <ViewCell>
                                    <Frame Padding="10" Margin="0,10,0,0" BorderColor="Black" HorizontalOptions="FillAndExpand" VerticalOptions="CenterAndExpand" >
                                        <Grid>
                                            <Grid.RowDefinitions>
                                                <RowDefinition Height="50*"/>
                                                <RowDefinition Height="50*"/>
                                            </Grid.RowDefinitions>
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="auto"/>
                                                <ColumnDefinition Width="auto"/>
                                                <ColumnDefinition Width="auto"/>
                                            </Grid.ColumnDefinitions>
                                            <Image Grid.RowSpan="2" Grid.Column="0" Source="{Binding SendReceiveTransactionImage}" />
                                            <Label Grid.Row="0" Grid.Column="1" Text="{Binding TransactionDescription}" TextColor="Black" FontAttributes="Bold" FontSize="Small"/>
                                            <Label Grid.Row="1" Grid.Column="1"  Text="{Binding DateAndTime}" TextColor="Black" HorizontalOptions="EndAndExpand" FontSize="13"/>
                                            <Label Grid.Row="0" Grid.Column="3" Text="{Binding TransactionAmount}" TextColor="Black" HorizontalOptions="EndAndExpand" FontAttributes="Bold" FontSize="Small"/>
                                            <Label Grid.Row="1" Grid.Column="3" Text="{Binding TransactionFee}" TextColor="Black" HorizontalOptions="EndAndExpand"/>
                                        </Grid>
                                    </Frame>
                                </ViewCell>
                            </DataTemplate>
                        </ListView.ItemTemplate>
                    </ListView>
                </Grid>
                <views:TabbedViewPage Grid.Row="2" HorizontalOptions="FillAndExpand"  BackgroundColor="Transparent"></views:TabbedViewPage>
            </Grid>
        </Grid>
    </ContentPage.Content>
</ContentPage>